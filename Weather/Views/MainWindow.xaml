<Window x:Class="Weather.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_New" />
                <MenuItem Header="_Open" />
                <MenuItem Header="_Save" />
                <Separator />
                <MenuItem Header="_Exit" />
            </MenuItem>
        </Menu>
        <ToolBarTray Background="DarkGray" DockPanel.Dock="Top" VerticalAlignment="Top">
            <ToolBar>
                <Button Command="New" Content="New" />
                <Button Command="Open" Content="Open" />
                <Button Command="Save" Content="Save" />
            </ToolBar>
            <ToolBar>
                <Button Command="Cut" Content="Cut" />
                <Button Command="Copy" Content="Copy" />
                <Button Content="Stations" Click="Button_Click" />
            </ToolBar>
        </ToolBarTray>
        <Button Content="Click" DockPanel.Dock="Top" Click="Button_Click_1"/>
        <ListBox DockPanel.Dock="Left" ItemsSource="{Binding Stations}" DisplayMemberPath="Name" Name="lb" SelectionChanged="lb_SelectionChanged"
                        HorizontalAlignment="Left" 
                         Width="150" />
        <DataGrid EnableColumnVirtualization="True" EnableRowVirtualization="True" Name="dg" CanUserAddRows="False" RowHeaderWidth="0" IsReadOnly="True" AutoGenerateColumns="False"  />

    </DockPanel>
</Window>