<UserControl x:Class="Weather.UserControls.StationSidePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Weather.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Border Grid.Row="0" Grid.Column="1" BorderBrush="DarkGray" BorderThickness="1">
        <DockPanel Style="{DynamicResource Header}">
            <DockPanel Style="{DynamicResource HeaderBG}" Height="25" DockPanel.Dock="Top">
                <Label Content="Station Details" Style="{DynamicResource HeaderLabel}" />
            </DockPanel>
            <Label FontWeight="Bold" FontSize="14" Content="Oregon LW301" DockPanel.Dock="Top" Style="{DynamicResource HeaderLabel}" />
            <Label Content="-27.3423" DockPanel.Dock="Top" Style="{DynamicResource HeaderLabel}" />
            <Label Content="153.456" DockPanel.Dock="Top" Style="{DynamicResource HeaderLabel}" />


            <DockPanel>
                <Label Content="Sensors" DockPanel.Dock="Top" Style="{DynamicResource HeaderLabel}" FontWeight="Bold"></Label>
                <ItemsControl Margin="5,0,0,0"  Width="Auto" ItemsSource="{Binding Station.Sensors}" >
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="{Binding Sensor.}" Style="{DynamicResource HeaderTextbox}"/>
                                <TextBlock Text="{Binding Correction}" Style="{DynamicResource HeaderTextbox}"/>
                                <TextBlock Text="{Binding Sensor.SensorType.Name}" Style="{DynamicResource HeaderTextbox}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DockPanel>






        </DockPanel>
    </Border>
</UserControl>
